# Environment Variables for Career Guide App
# Copy this file to .env and fill in values as needed

# ─────────────────────────────────────────────
# SUBMISSION SETTINGS
# ─────────────────────────────────────────────

# Submission endpoint (auto-configured for Netlify)
VITE_SUBMIT_ENDPOINT=/.netlify/functions/submit

# Recipient email for submissions
VITE_RECIPIENT_EMAIL=brainstein@protonmail.com

# ─────────────────────────────────────────────
# SERVER-SIDE ONLY (Netlify Environment Variables)
# ─────────────────────────────────────────────
# These must be set in Netlify's environment variables,
# NOT in this file. Never commit secrets!

# Resend API Key (get one at https://resend.com)
# RESEND_API_KEY=re_xxxxxxxxxxxx

# From email (must be verified in Resend, or use onboarding@resend.dev for testing)
# FROM_EMAIL=onboarding@resend.dev

# Recipient email (server-side override)
# RECIPIENT_EMAIL=brainstein@protonmail.com

# ─────────────────────────────────────────────
# LEGACY SETTINGS (for mailto fallback)
# ─────────────────────────────────────────────

# Email Mode (1 = mailto fallback only, 2 = serverless API)
# Mode 2 is now the default with the serverless submission
VITE_EMAIL_MODE=2

# ⚠️ NEVER commit API keys to the repo!
